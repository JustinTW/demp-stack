upstream internal_pma_http_hosts {
  least_conn;
  server pma:80;
  keepalive 64;
}

upstream internal_ppga_http_hosts {
  least_conn;
  server ppga:80;
  keepalive 64;
}

upstream internal_wordpress_http_hosts {
  least_conn;
  server wordpress:80;
  keepalive 64;
}
